# ===========================================
# Local Environment Variables (Developer Override)
# ===========================================
# Copy this file to .env.local to override defaults
# .env.local is gitignored and should never be committed

# Uncomment and configure if using actual AWS Cognito for local testing:
# VITE_USE_COGNITO=true
# VITE_COGNITO_DOMAIN=your-domain.auth.us-west-2.amazoncognito.com
# VITE_COGNITO_CLIENT_ID=your_client_id
# VITE_COGNITO_REDIRECT_URI=http://localhost:3000/callback
# VITE_COGNITO_LOGOUT_URI=http://localhost:3000

# Override API URL if needed:
# VITE_API_URL=http://localhost:8080
